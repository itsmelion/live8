(function ($) {
	// jquery goodness

	/*$('.overlay').css('overflow', 'hidden').bind('touchmove', function(e) {
							e.preventDefault()
					});
  $(function () {
  $('footer').css({bottom: '-100vh'});
		$("#home-wrap, .arrow").swipe( {
      swipe: function (event, direction, distance, duration, fingerCount, fingerData) {
				if (direction === 'up') {
          $("footer").animate(
            {
              bottom: '0'
            }
					);
				}
			},
			allowPageScroll:"none"
		});
		
		$("footer").swipe( {
			swipe:function(event, direction, distance, duration, fingerCount, fingerData) {

				if (direction === 'down') {
					console.log("Down")
					$("footer").animate(
						{
							bottom: '-100vh'
						}
					);
				};
			
			},
			allowPageScroll:"none"
		});
	
	});*/

})(jQuery);